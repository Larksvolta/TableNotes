<UserControl x:Class="TableNotes.Views.NotePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="SidebarBorderStyle" TargetType="Border">
                <Setter Property="Margin" Value="1"/>
                <Setter Property="CornerRadius" Value="3"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="RootLayout">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListView x:Name="NotesList"
                  ItemsSource="{Binding Notes}"
                  SelectedItem="{Binding SelectedNote, Mode=TwoWay}"
                  SelectionMode="Single"
                  BorderThickness="0,0,1,0"
                  BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis" MaxLines="1"/>
                        <TextBlock Text="{Binding ModifiedAt}"
                                   Foreground="{ThemeResource TextFillColorDisabledBrush}"
                                   FontSize="11"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TreeView x:Name="NotesTree"
                  BorderThickness="0,0,1,0"
                  BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"/>

        <Grid x:Name="BugTrackerSidebar" Visibility="Collapsed"
              Padding="12,12,12,0"
              BorderThickness="0,0,1,0"
              BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"/>

        <Grid Grid.Column="1">
            <Button x:Name="ToggleBtn" Width="20" Height="64"
                    VerticalAlignment="Center" Padding="0"
                    Background="Transparent" BorderThickness="0"
                    Click="OnToggleSidebar">
                <SymbolIcon Symbol="OpenPane"/>
            </Button>
        </Grid>

        <Grid Grid.Column="2" Padding="12,12,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="TitleBox" Grid.Row="0"
                     PlaceholderText="Title" FontSize="20" FontWeight="SemiBold"
                     Text="{Binding EditTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     BorderThickness="0" Background="Transparent"
                     TextWrapping="NoWrap"/>

            <Grid x:Name="TableViewContainer" Grid.Row="1"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,8,0,8">
                <Button x:Name="NewNoteBtn" Command="{Binding NewNoteCommand}" Content="+" FontSize="18"
                        Width="36" Height="36" Padding="0"/>
                <Button Command="{Binding SaveNoteCommand}" Content="Save"
                        Style="{StaticResource AccentButtonStyle}"/>
                <Button x:Name="DeleteNoteBtn" Command="{Binding DeleteNoteCommand}" Content="Delete"/>
                <Button Command="{Binding AddRowCommand}" Content="Add Row"/>
            </StackPanel>

            <InfoBar Grid.Row="3"
                     Message="{Binding StatusMessage}"
                     IsOpen="{Binding HasStatusMessage}"
                     Severity="Success" IsClosable="True"/>
        </Grid>
    </Grid>
</UserControl>